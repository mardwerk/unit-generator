# Generation instructions

Interpret the brief as a gameplay role. Produce a small complete family with distinct rank progression and a readable weakness. Include every referenced destination unless it already exists in the supplied roster. Only add a mixed recipe when the supplied context gives it a purpose. Respect maxRank when provided. Return JSON matching this definition's schema. This workflow needs no character research or tower-specific fields.
