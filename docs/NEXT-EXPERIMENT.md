# Next generation work

The September 22, 2026 decision is to complete concept authoring first and collect evidence during development. Numerical-backend extraction and PR 6 evaluator repairs are separate work, not prerequisites for the concept proof of concept.

The first implementation now exposes explicit concept mode through the core, CLI and UnitLab, with automatic attempt retention at the adapters. Offline integration tests exercise both public and synthetic progression, save/reload, revisions and readable rendering. Use [CLI concept authoring](CLI.md#qualitative-concepts) to try it. The retained failures have been classified in [PIPELINE-EVALUATION.md](PIPELINE-EVALUATION.md). The known-good external concept and an original public equivalent pass offline retention tests. A second complete Definition now exercises two four-tier automatic-only branches without crosspaths. Repeatable live design quality remains separate from those application checks.

Reuse the flexible drafting path through explicit Request, CLI and UnitLab mode selection. Supply the full intended skill and versioned public concept rules. The acceptance cycle is generation, save/reload, revision and rendering of a complete qualitative unit without a numerical blueprint or invented prices. Keep numerical operations and old artifacts usable. Offline tests establish application behavior; live outputs and human review are still required before claiming useful design quality.

Retain exact assembled requests, schemas, sources, resolved rules, effective generator files, model/settings and every raw attempt from the first development run. Record predicted positive/negative scenarios, meaningful changes and acceptance reasons separately from the player-facing sheet. [REFINEMENT.md](REFINEMENT.md) owns the compact evidence template and study protocol.

Before freezing a pilot, demonstrate two meaningfully different public or synthetic concept rulesets without generator edits, several references, and the complete operation cycle. Test independent attacks, finite hit budgets, event dependencies and cross-copy restrictions with original examples. Pair each deliberate defect with a valid control. Configuration of concept rules does not establish executable mechanic portability. Private project rules and character mappings stay outside the public repository.

Both PRs are merged as separate opt-in experiments. [PR 10](PR10-REVIEW.md) retains a caller-supplied interpretation on `planned-v1`; its correction excludes that record from provider output, preserves concept meaning in the prompt and checks exact citation spans. Citation coverage still does not establish interpretation fidelity.

[PR 6](PR6-REVIEW.md) retains compact numerical generation with an explicitly selected recipe. Its adaptation preserves the default route, supplies full revision context and the selected Definition, and replaces positional citations with explicit spans. The incomplete simulator and automatic name-based selection are excluded. Its earlier branch study remains historical evidence; the obsolete 24-call handoff must not be restarted.

Neither experiment is a replacement for concept authoring or a promoted default. Human-supplied interpretation and recipe selection are assistance factors, so label them separately in comparisons. Confirmed records unsupported by a route must produce an explicit error rather than disappear.

The separate [reshape plan](GENERATOR-RESHAPE.md) describes the Engine seam and an explicit character-organization artifact. The [refinement process](REFINEMENT.md) describes how to compare candidates and decide on one next correction. A matched free-model comparison remains necessary before claiming default superiority, but its budget and protocol should be set after the branch findings are addressed, not copied from the obsolete continuation task.

The latest main-branch failure batch remains useful development evidence: `.runs/unit-pipeline/final-benchmark-2026-09-21T21-50-33-334Z`. Its source-reading, typed-intent, mechanics and repair failures are documented in [pipeline evaluation](PIPELINE-EVALUATION.md). Compare claims and implementations before/after repair; do not diagnose every failure as missing Engine support. These cases are not untouched holdouts.

This roadmap distinguishes implemented operations from pending quality and study acceptance. The exact public BTD6 compatibility contract, public Apex acquisition, private form transitions and compatible Consumer remain later implementation requirements.
