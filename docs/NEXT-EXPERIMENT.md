# Next generation work

The September 22, 2026 decision is to complete concept authoring first and collect evidence during development. Numerical-backend extraction and PR 6 evaluator repairs are separate work, not prerequisites for the concept proof of concept.

The first implementation now exposes explicit concept mode through the core, CLI and UnitLab, with automatic attempt retention at the adapters. Offline integration tests exercise both public and synthetic progression, save/reload, revisions and readable rendering. Use [CLI concept authoring](CLI.md#qualitative-concepts) to try it. The remaining readiness question is repeatable design quality and preservation across real outputs; passing application tests does not settle it.

Reuse the flexible drafting path through explicit Request, CLI and UnitLab mode selection. Supply the full intended skill and versioned public concept rules. The acceptance cycle is generation, save/reload, revision and rendering of a complete qualitative unit without a numerical blueprint or invented prices. Keep numerical operations and old artifacts usable. Offline tests establish application behavior; live outputs and human review are still required before claiming useful design quality.

Retain exact assembled requests, schemas, sources, resolved rules, effective generator files, model/settings and every raw attempt from the first development run. Record predicted positive/negative scenarios, meaningful changes and acceptance reasons separately from the player-facing sheet. [REFINEMENT.md](REFINEMENT.md) owns the compact evidence template and study protocol.

Before freezing a pilot, demonstrate two meaningfully different public or synthetic concept rulesets without generator edits, several references, and the complete operation cycle. Test independent attacks, finite hit budgets, event dependencies and cross-copy restrictions with original examples. Pair each deliberate defect with a valid control. Configuration of concept rules does not establish executable mechanic portability. Private project rules and character mappings stay outside the public repository.

[PR 10](PR10-REVIEW.md) was re-reviewed at `c97fdad`. It now integrates an optional interpretation, but remains unmerged because the default provider schema exposes the new record, citation checks widen spans to documents, and the pinned prompt omits concept descriptions and relationships. Keep these integration corrections separate from the PR 6 evaluator work.

The PR 6 branch experiment is completed. Its review worktree was updated on September 22 from `d4240b2` to `3e5d73b`. Read the [current PR 6 review](PR6-REVIEW.md) before planning another experiment. The obsolete handoff and its 24-call task have been removed; that task must not be restarted. Main still uses `planned-v1`; no route was merged or promoted by the documentation review.

PR 6 now has a source-aware option, technique grouping, revision context and a reference-wave simulator. Its report includes successful samples and a failed retry sequence. The review reproduced 30 offline tests, but also found that the simulator ignores follow-ups, target priority, delivery and boosted attack cadence in relevant probes. The source grouping still fixes workhorse/burst/support assignments, and branch citations remain positional. Passing the new gate does not resolve the user's objections about character identity or core replacement.

For a later PR 6 continuation, separate evaluator correctness from generator quality. On the PR branch or an isolated follow-up, correct or remove publication gating for unmodeled mechanics, pass the supplied Definition into evaluation, and add a small scenario for each claimed supported interaction. Preserve unknown/not-simulated outcomes. A valid follow-up must not be rejected because the evaluator ignores its damage. A faster active attack must actually create more attack opportunities. Status effects must follow their declared application and expiry rules.

In any compact route proposed for adoption, replace positional source assignment with explicit evidence IDs tied to the affected design choice. Preserve previous mechanics and confirmed interpretation when revising. Keep source-derived facts separate from regex-based adaptation hints and map-unit constants. These are offline correctness improvements; no new live batch is needed to establish the current defects.

The separate [reshape plan](GENERATOR-RESHAPE.md) describes the Engine seam and an explicit character-organization artifact. The [refinement process](REFINEMENT.md) describes how to compare candidates and decide on one next correction. A matched free-model comparison remains necessary before claiming default superiority, but its budget and protocol should be set after the branch findings are addressed, not copied from the obsolete continuation task.

The latest main-branch failure batch remains useful development evidence: `.runs/unit-pipeline/final-benchmark-2026-09-21T21-50-33-334Z`. Its source-reading, typed-intent, mechanics and repair failures are documented in [pipeline evaluation](PIPELINE-EVALUATION.md). Compare claims and implementations before/after repair; do not diagnose every failure as missing Engine support. These cases are not untouched holdouts.

This roadmap records planned acceptance, not completed generation or study results. The exact public BTD6 compatibility contract, public Apex acquisition, private form transitions and compatible Consumer remain later implementation requirements.
