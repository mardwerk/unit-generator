# Next generation work

The September 22, 2026 priority is to establish useful concept authoring through development evidence. The core, CLI and UnitLab already expose explicit concept mode with adapter attempt retention. Numerical-backend extraction and PR 6 evaluator repairs remain separate work, not concept prerequisites. Numerical `planned-v1` remains the default.

Offline integration and retention tests cover public and synthetic progression, generation, save/reload, revisions and readable rendering. The known-good external concept and an original public equivalent pass retention tests. A second complete Definition supplies two four-tier automatic-only branches without crosspaths. These checks establish application behavior, not repeatable live design quality or executable mechanic portability. Try [CLI concept authoring](CLI.md#qualitative-concepts).

Next, use the classified [retained concept failures](PIPELINE-EVALUATION.md) to select one correction under [REFINEMENT.md](REFINEMENT.md). Preserve the full intended skill, versioned public concept rules and explicit Request, CLI and UnitLab selection. Acceptance still requires live complete qualitative units, save/reload, revision, rendering and human review, without numerical blueprints or invented prices. Keep numerical operations and old artifacts usable.

Before freezing a pilot, collect several references across the two meaningfully different public or synthetic rulesets without generator edits. The existing offline coverage is a starting point. Evaluate independent attacks, finite hit budgets, event dependencies and cross-copy restrictions using original examples; pair deliberate defects with valid controls. Keep private project rules and character mappings outside this repository.

Retain exact assembled requests, schemas, sources, resolved rules, effective generator files, model/settings and every raw attempt from the first development run. Record predicted positive/negative scenarios, meaningful changes and acceptance reasons separately from player-facing sheets. [REFINEMENT.md](REFINEMENT.md) owns the evidence template, candidate comparison and study protocol.

PR 10 and PR 6 are merged, separate opt-in experiments. [PR 10](PR10-REVIEW.md) retains a caller-supplied interpretation on `planned-v1`, excludes it from provider output, preserves concept meaning in prompts and checks exact citation spans. Citation coverage does not establish interpretation fidelity. [PR 6](PR6-REVIEW.md) uses explicitly selected numerical recipes, full revision context, the supplied Definition and explicit citation spans. Automatic name-based selection and the incomplete simulator are excluded. Neither replaces concept authoring or promotes a default. Label human interpretation and recipe selection as assistance factors. Routes must explicitly reject unsupported confirmed records.

[GENERATOR-RESHAPE.md](GENERATOR-RESHAPE.md) retains the proposed Engine boundaries and character-organization work. A matched free-model comparison remains necessary before claiming default superiority. Set the budget and protocol after addressing relevant branch findings. PR 6's completed branch study is historical evidence; never restart the obsolete 24-call handoff.

Retain main's failure batch `.runs/unit-pipeline/final-benchmark-2026-09-21T21-50-33-334Z` as development evidence. [Pipeline evaluation](PIPELINE-EVALUATION.md) classifies source-reading, typed-intent, mechanics and repair failures. Compare claims and implementations before/after repair; not every failure means missing Engine support. These cases are not untouched holdouts.

Exact public BTD6 compatibility, public Apex acquisition, private form transitions and a compatible Consumer remain later implementation requirements.
